import { memo } from "react";

const Login = ({ onLogin }) => {
    // const handleLogin = () => {
    //     // Xử lý đăng nhập
    //     onLogin();
    // };

    // return (
    //     <div>
    //         <h2>Đăng nhập</h2>
    //         <button onClick={handleLogin}>Đăng nhập</button>
    //     </div>
    // );
    return (
        <div>
            <h1>xxx</h1>
        </div>
    );
}




export default memo(Login);